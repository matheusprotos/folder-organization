export interface NormalizedLocale {
  en_US: string;
  pt_BR: string;
  en: string;
  pt_US: string;
}

export interface Language {
  app: object;
  button: object;
  common: object;
  heading: object;
  input: object;
  placeholder: object;
  validation: object;
}
